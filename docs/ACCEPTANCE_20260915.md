# 2026-09-15 验收记录

## 自动验证

- `cargo test --workspace --all-targets --offline`：684 项通过。
- `cargo clippy --workspace --all-targets --offline -- -D warnings`：通过。
- 已覆盖串口释放互斥、单事务提交、延迟回执、发送错误详情、重复提交、清理占用、设备代次隔离、草稿保留、短信自动同步和无线历史边界。
- 发送超过普通监测期限的测试先建立设备就绪，再施加短监测预算，避免线程调度导致前置设备查询超时。

## 窗口检查

使用真正的 egui Windows 窗口和明确标记的模拟数据检查短信列表、阅读、编辑、确认及无线观测布局。预览不连接设备、不发送短信。

已检查 1100×760 与 800×600 的主要短信状态，并检查过 150% 缩放页面。未完成所有五页、所有状态在 100% / 150% / 200% Windows 系统缩放下的完整人工验收矩阵。布局测试和截图不等于实机短信验收。

## 实机边界

用户已确认此前短信版本可用；本次新增自动同步、无线观测的所有固件兼容性未完成实机验收。软件提交成功需收到 +CMGS 和最终 OK；送达需接收端确认。自动测试不发送真实短信。

无线观测复用已有只读查询；缺失指标保持未知，SINR 保留原始值，未实现锁频、写 NV 或重置网络。历史最多 120 个样本和 20 条小区变化，设备或 SIM 变化后清空。

## 分发

Windows x64 开发版本，未进行可信证书签名。便携包与 MSIX 均附 SHA-256；GitHub 发布工作流只有通过验证和包校验才创建 Release。CI 的依赖审计结果以 GitHub Actions 实际运行记录为准。

云端编译、Clippy 和完整测试亦已通过。依赖审计包含一项明确记录的停止维护例外；详见 DEPENDENCY_REVIEW.md。

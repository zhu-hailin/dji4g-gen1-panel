# 0.1.4 模块管理验收记录（2026-09-22）

## 基线与范围

实际目录为 `.worktrees/dji4g-gen1-panel`，分支 `feature/dji4g-gen1-panel`，本地 HEAD `8882c5651b54262ab332b180b379a90eac8b15a8`。完整基线包含开始时已有的未提交改动。清单、SHA-256、原件备份及本轮日志位于本地 `docs/implementation-20260922/20260922-174051/`，不将原件备份和本机路径清单公开上传。

按用户后续要求递增为 0.1.4，并在验证后更新 GitHub 最新 Release。远端发布基线为 main `d72f46b063480024f37d3ebc2b65cb38c4623a7c`（v0.1.3）；只更新模块管理源码、相关测试和发布说明。保留远端无关内容，不发布个人控制中心、厂商驱动、现有本地备份或调试脚本。

## 修改与回归证据

用户在实施中追加首次连接引导，并授权使用电脑已有驱动。本轮将其一并纳入0.1.4：自动检查、随时跳过、记住选择、设置中重开；本地包复用已核验驱动，公共下载入口引导至官方说明及支持，不自动下载或安装。

| 问题/功能 | 实现与回归 |
| --- | --- |
| 长短信引用号截断、混组、内容去重 | `SmsConcatReference` 保留8/16位类型与完整值；分组含设备、SIM、存储、方向；长度前缀 payload 指纹含完整分片头。domain身份测试、protocol解码、application multipart/store测试先复现再修复。 |
| 合并行丢失真实索引 | `SmsDisplayMessage` 保存排序去重的真实分片身份，稳定展示ID随内容/分片集合变化；冲突禁删，缺片明确显示删除已读取分片。 |
| 整条短信删除 | 冻结整组身份、异步逐片执行；同会话核对PDU模式、CPMS mem1、CMGR指纹后只发一次CMGD。最终OK才确认成功，失败/取消/未知停止。`sms_delete_workflows` 8项覆盖成功、部分失败、未知、上下文变化、期限、无端口、互斥与关闭清理。 |
| 串口执行与清理 | 平台mock覆盖命令次数、格式/存储/索引内容变化、取消、写入不确定性和租约；实际I/O共享取消及30秒绝对期限，关闭等待3秒，清理未完继续忙碌。 |
| 工具占用时写信一直等待 | Controller统一返回`sms:busy`；compose只释放尚未获得请求ID且收到新拒绝的pending，保留草稿。controller与compose各有回归。 |
| 同步读取超时提前释放忙碌 | 独立审查复现后修复；保留旧worker完成通道，迟到结果丢弃，真正结束前不接受删除或自动刷新。`sms_delete_review`覆盖read/list及设备代次变化。 |
| 索引复用后刷新无法恢复 | 仅成功全量列表明确覆盖的槽位可清除过时payload，同批次冲突仍保留；不猜测未列出的存储位置。`sms_slot_refresh` 3项先失败后通过。 |
| 操作条件与清空 | 快照`serial_work_busy`及共用修复可用性视图；清空实际移除历史，保留能力/资料/当前任务，后来完成的新记录正常显示。 |
| 概览与引导 | 首屏紧凑读数、图表切换、折叠引导；纯视图模型区分检测中、过期、未执行、主动探测关闭、失败与全部通过。窄布局回归通过。 |
| 缺SIM/关闭探测误显示可用 | `honest_readiness`先复现后修复：AT可用不再等同蜂窝就绪，缺SIM明确阻断；关闭探测撤销旧公网/DNS证明，重新开启等待新结果。4项回归通过。 |
| 接口超时误展开驱动安装 | 先复现AT超时会展开安装，再改为仅有新鲜Code 28明确证据时默认展开；无证据保持折叠，仍可手动查看。 |
| 发送结果视觉 | 进度/已提交/失败/未知使用文字、标记及不同色调；未知原先中性色回归先失败后修复，不增加重试。 |
| 回复与单项重查 | 回复只预填通过现有号码校验的原地址，正文为空；保护已有草稿和发送确认。能力行只提交对应的一个RunToolRead，旧值标时间及查询中，入队失败不伪造执行。 |
| 首次连接引导 | 生产首次会话启用，复用后台检查；跳过/进入持久化完成标记，设置可重开，旧配置兼容，首次不隐藏到托盘。无设备/待查/关闭探测不误判驱动失败；保存失败仍进入并提示。内置驱动使用原确认与安装检查。 |

## 视觉验收

尺寸覆盖800×600的100%/150%、1100×760的100%、1440×1000的100%。使用Noop命令sink和模拟快照，不连接设备。

最终概览/查询/短信操作区截图保存在本地证据目录`screenshots-final`，引导与缺SIM修正文案在`screenshots-onboarding-final`；发送四态、部分删除和草稿替换使用初始`screenshots`中的对应场景。健康首屏五项摘要可见，缺串口/缺SIM/DNS失败/探测关闭文案分别核对。单项查询展开行显示查询中、旧采集时间并保留其他项；小窗短信内部滚动后回复/删除按钮完整。

截图审查曾发现并修正模拟状态被健康快照覆盖、缺SIM误显示内部错误，以及引导底部对比度问题。中间排错目录不作为最终状态证据。截图不证明真实硬件或短信行为。

## 驱动获取核验

[DJI官方FAQ第21项](https://repair.dji.com/help/content?customId=01700008285&lang=en&paperDocType=ARTICLE&re=US&spaceId=17)仍提供一代模块Windows使用说明；其中旧分享链接在2026-09-22实际HTTP核验返回404。引导提供官方说明/支持入口，不把失效地址称为可用直链，不从未知镜像替换驱动。用户已有的本地包继续使用固定哈希与CAT签名核验后打入本地独立版。

## 自动检查

- `cargo fmt --all -- --check`：通过。
- `cargo clippy --workspace --all-targets --offline --locked -- -D warnings`：通过。
- `cargo test --workspace --all-targets --offline --locked --no-fail-fast`：**879项通过，0失败**。
- `git -c core.whitespace=cr-at-eol diff --check`：通过（仅Git换行提示）。
- 驱动结果与安装命令的纯模拟PowerShell回归、MSIX dry-run：通过；没有安装驱动。
- `cargo build --workspace --release --offline --locked --target x86_64-pc-windows-msvc`：通过。

## 本地独立版

新目录：`dist/20260922-module-management-184307/`。

文件：`大疆4G模块管理-本轮验收版.exe`，11,071,488字节。

SHA-256：`CF2DF19CDFEA73DF0F587B9CCA30515E1B8C5826F6AE799E3AB24B5E703E232A`。

驱动来源为现有 `dist/local-driver-kit-20260918`，打包前安装器`--check`成功。本轮release panel/helper/driver-setup与新payload逐项SHA-256一致，详见交付目录`binary-hashes.json`；没有使用旧payload面板。

在交付目录下隔离进程级LOCALAPPDATA，连续两次`--verify-bundle`均成功校验17项资源，覆盖首次解包和重复校验。未启动实机面板、未安装驱动，原有面板进程保留。另生成并回读校验不含驱动便携ZIP。

## 尚未进行的实机验收与边界

- 真实短信送达、实机整条/部分删除、USB重新枚举及新电脑驱动兼容性，均未以自动测试或模拟截图冒充通过。
- 已提交给模块不等于收件人收到；结果未知不自动重发/重删。删除前CMGR核验可能改变模块已读状态。
- 按计划不引入按时间猜测归属。相同设备/SIM/存储/发件人长期复用同一个拼接引用号而只留下互补残片时，仅靠现有UDH无法证明跨时间消息归属。
- 成功刷新只清理本次明确覆盖槽位的旧内容；空列表或未列出的存储位置不会被猜测为已清空。
- 公开Release继续不含厂商驱动，MSIX仍为未签名开发包；本地含驱动独立版不公开上传。

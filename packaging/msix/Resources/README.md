# MSIX resources

The manifest declares the `zh-CN` and `en-US` resource qualifiers.  User-visible
runtime strings remain in the panel's compile-time catalogs; this directory is
reserved for future localized package resources and is intentionally free of
device identifiers, credentials, or generated signing material.
